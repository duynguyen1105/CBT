import { createStyles, makeStyles } from "@material-ui/core/styles";

export default makeStyles(() =>
  createStyles({
    container: {
      width: "100%",
      backgroundColor: "#fff",
      borderRadius: 5,
      paddingTop: 16,
    },
  })
);
